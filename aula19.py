from random import randint

loteria = ["



while i < len (loteria):
    #Gera número
    num = randint (1,60)

    #variavel de controle do laço que verifica
    k = 0
    #laço para verificação do número
    while k <i



        #incremento da posição
        k += 1
    #Armazena
    loteria [i] =
    #Incremento da posição
    i += 1
